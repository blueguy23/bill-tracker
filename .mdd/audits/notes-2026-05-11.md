# MDD Audit Notes — 2026-05-11

### [01] Monthly Summary
**Files read:** src/app/summary/page.tsx, src/components/MonthlySummary.tsx, tests/unit/monthly-summary.test.ts
**Findings:**
- MEDIUM: `computeMonthStats` — recurring bills included in every month regardless of per-month payment state; `isPaid` is a single boolean, not per-month
- MEDIUM: Silent fetch error swallowing — `.catch(() => {})` discards errors from `/api/v1/summary`
- LOW: Non-ok response resolves to `null` with no user feedback
**Test coverage:** 12 / 1 function (computeMonthStats well-covered; no component tests)
**Doc accuracy:** Recurring bill double-count not documented as known gap

### [02] Payment History
**Files read:** src/types/payment.ts, src/adapters/payments.ts, src/handlers/payments.ts, src/app/api/v1/bills/[id]/payments/route.ts, src/components/PaymentHistoryModal.tsx, tests/unit/payment-history.test.ts
**Findings:**
- MEDIUM: No UUID format validation on `billId` path parameter
- MEDIUM: Payment records not deleted when bill is deleted (orphans)
- LOW: Hard limit of 500 payments with no pagination or truncation signal
**Test coverage:** 11 tests, good coverage. Missing: DELETE bill orphan cleanup, invalid billId format
**Doc accuracy:** No discrepancies

### [03] SimpleFIN Core Sync
**Files read:** src/lib/simplefin/client.ts, src/lib/simplefin/transform.ts, src/lib/simplefin/types.ts, src/adapters/accounts.ts, src/adapters/syncLog.ts, src/handlers/sync.ts, src/app/api/v1/sync/route.ts, src/app/api/v1/sync/status/route.ts, src/app/api/v1/sync/historical/route.ts
**Findings:**
- CRITICAL: `runHistoricalImport` has no quota guard — can blow past SIMPLEFIN_QUOTA_GUARD with 3 fetches
- CRITICAL: `markTransfersById` uses sequential awaits in a loop — partial failure leaves inconsistent state
- HIGH: `listCategoryRules` called once per transaction inside sync loop (300 DB reads instead of 1)
- MEDIUM: `RawSFINAccount.org` typed as required but documented as sometimes absent
- MEDIUM: `incrementUrlUnits` silently no-ops when no sync log exists for today
- MEDIUM: `nextScheduledSync` only handles simple `"M H * * *"` cron format
- LOW: Fire-and-forget post-sync handlers with no error boundary
- LOW: `getCashFlowThisMonth` uses `$lte` instead of `$lt` for end boundary
**Test coverage:** 0 dedicated unit tests for this feature
**Doc accuracy:** Quota guard absence means documented limit can be silently violated

### [04] Budget & Alerts
**Files read:** src/types/budget.ts, src/adapters/budgets.ts, src/adapters/quickAdd.ts, src/handlers/budgets.ts, src/lib/budget/engine.ts, src/lib/budget/rollover.ts, src/app/api/v1/budgets/route.ts, src/app/api/v1/budgets/[category]/route.ts
**Findings:**
- CRITICAL: Budget notification passes `spent` instead of `burnRate.linearProjectedTotal` to `computeCategoryStatus` — split-brain between displayed status and notification trigger
- HIGH: Fire-and-forget `checkBudgetNotifications` with no error boundary on upstream DB calls
- HIGH: `applyMonthEndRollover` has no idempotency guard — double-run compounds rollover
- LOW: `Infinity` passes budget amount validation
**Test coverage:** Good engine coverage. No tests for rollover idempotency or notification trigger path
**Doc accuracy:** `src/lib/budget/dedupe.ts` listed in docs but does not exist

### [05] Credit Health
**Files read:** src/types/credit.ts, src/adapters/credit.ts, src/handlers/credit.ts, src/app/api/v1/credit/summary/route.ts, src/app/credit/page.tsx, src/components/CreditAccountCard.tsx, src/components/CreditHealthScore.tsx, src/components/RecentPaymentsPanel.tsx, src/components/CreditView.tsx, src/components/Sidebar.tsx
**Findings:**
- HIGH: "Recent Payments" panel queries `amount < 0` (purchases) instead of `amount > 0` (actual payments) — shows wrong data
- HIGH: `paydownTo30Pct` uses negative balance directly — `Math.max(0, negative - positive)` always returns 0
- HIGH: `paydownToTarget` same sign error — statement close alerts always show $0
- MEDIUM: `CreditAccountCard` displays raw negative balance as "Balance"
- LOW: Refresh button renders but has no onClick handler
**Test coverage:** Good unit coverage for health score. No tests for `checkCreditAlerts` sign logic
**Doc accuracy:** `CreditView` is dead code — page renders different components

### [06] Discord Notifications
**Files read:** src/types/notification.ts, src/adapters/notificationLog.ts, src/lib/discord/webhook.ts, src/lib/discord/embeds.ts, src/handlers/notifications.ts, src/handlers/notificationDigest.ts, src/app/api/v1/notifications/test/route.ts, src/app/api/v1/notifications/digest/route.ts, src/app/settings/page.tsx, src/components/SettingsView.tsx
**Findings:**
- HIGH: Notification test and digest endpoints use GET for state-mutating operations
- HIGH: `notifySyncCompleted` uses `Date.now()` as dedup key — never deduplicates, no cooldown entry
- MEDIUM: Webhook send failure returns `reason: 'no_webhook'` instead of `'send_failed'`
- LOW: `NEXT_PUBLIC_DISCORD_CONFIGURED` is build-time only — can diverge from runtime state
**Test coverage:** Good coverage for core notification flow. No tests for `checkCreditAlerts`, `notifyPriceIncrease`
**Doc accuracy:** Settings UI says budget warning at 80% but engine threshold is 90%

### [07] Subscription Detection
**Files read:** src/types/subscription.ts, src/adapters/subscriptions.ts, src/lib/subscriptions/detect.ts, src/lib/subscriptions/normalize.ts, src/lib/subscriptions/autoMatch.ts, src/handlers/subscriptions.ts, src/app/api/v1/subscriptions/route.ts, src/app/api/v1/subscriptions/dismiss/route.ts, src/app/api/v1/subscriptions/matches/route.ts
**Findings:**
- CRITICAL: `handleAnchorSubscription` missing `lastCharged` validation — `new Date(undefined)` produces NaN dueDate
- HIGH: `dismissSubscription` catches all DB errors, not just duplicate-key — masks real failures
- HIGH: `handleListSubscriptions` fires side-effects (updateLastChargedAmount, recordCharge) on every GET — spams chargeHistory
- MEDIUM: `inferCategory` fallback is `'subscriptions'` instead of `'other'`
- MEDIUM: `findAutoMatches` no minimum length for substring match — single-char names match everything
- MEDIUM: `listDismissedSubscriptions` hard caps at 1000 with no pagination
**Test coverage:** 3 test files for classify/detect/normalize. Zero tests for anchor, dismiss, or matches endpoints
**Doc accuracy:** CLAUDE.md lists yearly interval as a gap but it's already implemented

### [08] Credit Optimizer & Statement Alert
**Files read:** src/types/creditAdvisor.ts, src/adapters/accountMeta.ts, src/handlers/creditAdvisor.ts, src/handlers/creditSettings.ts, src/app/api/v1/credit/advisor/route.ts, src/app/api/v1/credit/settings/route.ts, src/components/CreditAdvisorPanel.tsx, src/components/UtilizationTrendChart.tsx, src/components/CreditView.tsx, src/app/credit/page.tsx, src/app/settings/page.tsx
**Findings:**
- CRITICAL: `POST /api/v1/credit/settings` has no field-level validation — accepts arbitrary values (day 999, negative limits)
- HIGH: `computeUtilizationTrend` balance reconstruction sign logic may underreport historical utilization
- HIGH: AZEO excludes accounts missing `availableBalance` with no user explanation
- MEDIUM: UTC/local timezone mismatch in `nextStatementCloseDate`
- MEDIUM: `CreditView` component is dead code — not rendered by credit page
- MEDIUM: `credit/page.tsx` calls handlers that return NextResponse then deserializes — violates Rule 10
**Test coverage:** Good unit coverage for advisor functions. Zero tests for settings POST validation
**Doc accuracy:** No discrepancies

### [09] Annual Amortization
**Files read:** src/types/subscription.ts, src/lib/simplefin/types.ts, src/lib/subscriptions/detect.ts, src/adapters/cashFlowHistory.ts, src/app/api/v1/transactions/[id]/amortize/route.ts, src/components/TransactionsView.tsx, src/components/SubscriptionsView.tsx, src/app/page.tsx
**Findings:**
- HIGH: `handleAmortizeConfirm` removes subscription from UI without creating bill — reappears on next load
- HIGH: `TransactionsView` has local `isTransfer` function diverged from canonical `classifyTransfer`
- MEDIUM: Amortize route hardcodes collection name instead of importing shared constant
- MEDIUM: Amortization always divides by 12 regardless of actual subscription interval
- LOW: No tests for amortize route or normalized cash flow path
**Test coverage:** 0 unit tests for amortization
**Doc accuracy:** CLAUDE.md lists yearly interval as gap but it's implemented
